# Headless-Wan2GP project requirements
# Only includes dependencies NOT already in Wan2GP/requirements.txt
# Note: Install Wan2GP/requirements.txt first, then this file

# ===== Additional Dependencies for Headless Operation =====
huggingface_hub>=0.25.0  # For reliable model/LoRA downloads with checksums (different from Wan2GP version)
safetensors>=0.4.0  # For LoRA integrity verification
opencv-python-headless  # For headless environments
librosa  # Different version requirement than Wan2GP's librosa==0.11.0
Pillow  # For image processing in headless tasks
mediapipe

# ===== Web UI and API =====
fastapi  # Often needed for gradio apps
python-multipart  # For file uploads

# ===== Database and Storage =====
supabase  # For Supabase DB (via RPC) and Storage
python-dotenv  # For .env file management

# ===== Utilities =====
requests  # For downloading LoRAs and videos
replicate  # For magic edit tasks via Replicate API